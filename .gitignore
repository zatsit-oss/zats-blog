# Dependencies
/node_modules

# Production
/build

#local
/.idea
.firebaserc
.firebase/

# Generated files
.docusaurus
.cache-loader

# Astro: build output and generated types. Both are derived from the sources
# and from the content repository, so neither belongs in git.
dist/
.astro/

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

blog/**
docs/**
blog/authors.yml
static/img/authors/**
# Claude Code: the rules and skills are project standards, shared like any
# other convention file. Only the per-developer settings stay local.
# Same arrangement as the zats-websites monorepo.
.claude/*
!.claude/rules/
!.claude/skills/

sync-content.sh
dev.sh
watch-content.mjs

static/assets/*
.secrets

# Captures de travail, non versionnées
img.png

# Notes de reprise, locales : l'état courant du chantier est dans
# PLAN-MIGRATION.md, versionné, pas dans un fichier de brouillon.
REPRISE.md
