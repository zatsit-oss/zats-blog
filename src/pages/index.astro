---
/**
 * First page of the article listing, served at the root.
 *
 * Pages 2 and beyond live in src/pages/page/[page].astro rather than coming
 * from a single paginate() call, because Astro's pagination would emit /2/ and
 * the Docusaurus URL to preserve is /page/2/.
 */
import Layout from '../layouts/Layout.astro';
import PostList from '../components/PostList.astro';
import { sortedPosts } from '../utils/posts';
import { SITE_TITLE, SITE_DESCRIPTION, POSTS_PER_PAGE } from '../consts';

const posts = await sortedPosts();
const totalPages = Math.max(1, Math.ceil(posts.length / POSTS_PER_PAGE));
---

<Layout title={SITE_TITLE} description={SITE_DESCRIPTION}>
  <PostList
    posts={posts.slice(0, POSTS_PER_PAGE)}
    heading="Le blog de zatsit"
    intro={SITE_DESCRIPTION}
    current={1}
    total={totalPages}
    nextUrl={totalPages > 1 ? '/page/2/' : undefined}
  />
</Layout>
